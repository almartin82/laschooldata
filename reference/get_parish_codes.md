# Louisiana Parish Codes

Returns a data frame mapping Louisiana LEA codes to parish names.
Louisiana has 64 parishes (equivalent to counties) plus special
districts.

## Usage

``` r
get_parish_codes()
```

## Value

Data frame with lea_code and parish_name columns
