# Download file with retry logic

Download file with retry logic

## Usage

``` r
download_with_retry(url, destfile, max_retries = 3)
```

## Arguments

- url:

  URL to download

- destfile:

  Destination file path

- max_retries:

  Maximum number of retry attempts

## Value

TRUE if successful
